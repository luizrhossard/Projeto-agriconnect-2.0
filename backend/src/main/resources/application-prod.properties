# Profile de Producao
# Ativar: spring.profiles.active=prod
# TODAS as variaveis sensiveis DEVEM ser definidas via variaveis de ambiente

# DataSource
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Server
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-stacktrace=never

# JWT - OBRIGATORIO via variavel de ambiente (sem fallback)
app.jwt.secret=${APP_JWT_SECRET}
app.jwt.expiration=${APP_JWT_EXPIRATION:86400000}
app.jwt.issuer=agricultura-api

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS}

# Logging
logging.level.com.agricultura=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate=WARN
