# Profile de desenvolvimento

# DataSource local
spring.datasource.url=jdbc:postgresql://localhost:5432/agricultura_dev
spring.datasource.username=dev_user
spring.datasource.password=dev_password_!2x_2024
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never

# JWT (somente desenvolvimento)
app.jwt.secret=cHJveWV0by1zcHJpbmctYWdyaWN1bHR1cmEtc2VjcmV0LWtleS0yMDI0LW11aXRvLXNlZ3VyYS1lLWxvbmdhLTEyMzQ1Njc4OTA=
app.jwt.expiration=86400000
app.jwt.issuer=agricultura-api

# CORS
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000

# Cookies / features para dev
app.security.cookie.secure=false
app.security.cookie.same-site=Lax
app.swagger.enabled=true
app.seeder.enabled=true

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Rate Limiting
app.rate-limit.enabled=true
app.rate-limit.requests-per-minute=120

# Logging
logging.level.com.agricultura=DEBUG
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG
